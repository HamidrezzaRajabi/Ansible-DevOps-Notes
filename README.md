## 🐳 Docker Lab Environment

A local multi-node Ansible lab using Docker Compose is available in [`docker/ansible-lab/`](./docker/ansible-lab/).

It spins up an Ansible control node + Ubuntu and Rocky Linux targets with automatic SSH key distribution. See the [lab README](./docker/ansible-lab/README.md) for setup instructions.
